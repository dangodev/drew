{
  "name": "drew",
  "version": "1.0.2",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "dev": "preact watch",
    "serve": "preact build && preact serve",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev"
  },
  "dependencies": {
    "matter-js": "^0.14.2",
    "poly-decomp": "^0.3.0",
    "preact": "^10.4.4"
  },
  "devDependencies": {
    "if-env": "^1.0.4",
    "preact-cli": "^2.2.1"
  }
}
